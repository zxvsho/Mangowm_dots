# reload config
bind=CTRL+SUPER,Return,spawn_shell,bash ~/.config/mango/scripts/config_check.sh
bind=SUPER+SHIFT,Return,reload_config

# menu and terminal
bind=Alt,Return, spawn, kitty
bind=SUPER,Return,spawn,foot
bind=Alt,space,spawn,bash ~/.dots/scripts/rofi_launch.sh
bind=SUPER+Alt,space,spawn, walker

# exit super+m
# bind=SUPER,code:58,quit
bind=SUPER,m,quit
# killclient super+q
# bind=ALT,code:24,killclient,
bind=ALT,q,killclient,

# switch window focus
bind=SUPER,Tab,focusstack,next
bind=SUPER,u,focuslast
bind=ALT,Left,focusdir,left
bind=ALT,Right,focusdir,right
bind=ALT,Up,focusdir,up
bind=ALT,Down,focusdir,down

# swap window
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right

# movewin
bind=CTRL+SHIFT,Up,movewin,+0,-50
bind=CTRL+SHIFT,Down,movewin,+0,+50
bind=CTRL+SHIFT,Left,movewin,-50,+0
bind=CTRL+SHIFT,Right,movewin,+50,+0


# resizewin
bind=CTRL+ALT,Up,resizewin,+0,-50
bind=CTRL+ALT,Down,resizewin,+0,+50
bind=CTRL+ALT,Left,resizewin,-50,+0
bind=CTRL+ALT,Right,resizewin,+50,+0

# switch window status
bind=SUPER,g,toggleglobal,
bind=ALT,Tab,toggleoverview,0
bind=ALT,w,togglefloating,
bind=ALT,f,togglemaximizescreen,
bind=ALT,p,togglefullscreen,
bind=ALT+SHIFT,f,togglefakefullscreen,
bind=SUPER,i,minimized,
bind=SUPER,o,toggleoverlay,
bind=SUPER+SHIFT,I,restore_minimized
bind=ALT,z,toggle_scratchpad

# scroller layout
bind=ALT,e,set_proportion,1.0
bind=ALT,x,switch_proportion_preset,
bind=ALT,x,switch_proportion_preset,prev
bind=alt+super+ctrl,Left,scroller_stack,left
bind=alt+super+ctrl,Right,scroller_stack,right
bind=alt+super+ctrl,Up,scroller_stack,up
bind=alt+super+ctrl,Down,scroller_stack,down

#dwindle layout
bind=alt+shift,Return,dwindle_toggle_split_direction

# tile layout
bind=SUPER,e,incnmaster,1
bind=SUPER,t,incnmaster,-1
bind=ALT,s,zoom,

# switch layout
bind=CTRL+SUPER,i,setlayout,tile
bind=CTRL+SUPER,l,setlayout,scroller
bind=SUPER,n,switch_layout

# tag switch
bind=SUPER,Left,viewtoleft,0
bind=CTRL,Left,viewtoleft_have_client,0
bind=SUPER,Right,viewtoright,0
bind=CTRL,Right,viewtoright_have_client,0
bind=CTRL+SUPER,Left,tagtoleft,0
bind=CTRL+SUPER,Right,tagtoright,0

bind=ctrl,1,view,1,0
bind=ctrl,2,view,2,0
bind=ctrl,3,view,3,0
bind=ctrl,4,view,4,0
bind=ctrl,5,view,5,0
bind=ctrl,6,view,6,0
bind=ctrl,7,view,7,0
bind=ctrl,8,view,8,0
bind=ctrl,9,view,9,0

bind=Alt,1,tag,1,0
bind=Alt,2,tag,2,0
bind=Alt,3,tag,3,0
bind=Alt,4,tag,4,0
bind=Alt,5,tag,5,0
bind=Alt,6,tag,6,0
bind=Alt,7,tag,7,0
bind=Alt,8,tag,8,0
bind=Alt,9,tag,9,0

bind=ctrl+Super,1,toggletag,1
bind=ctrl+Super,2,toggletag,2
bind=ctrl+Super,3,toggletag,3
bind=ctrl+Super,4,toggletag,4
bind=ctrl+Super,5,toggletag,5
bind=ctrl+Super,6,toggletag,6
bind=ctrl+Super,7,toggletag,7
bind=ctrl+Super,8,toggletag,8
bind=ctrl+Super,9,toggletag,9

bind=Super,1,toggleview,1
bind=Super,2,toggleview,2
bind=Super,3,toggleview,3
bind=Super,4,toggleview,4
bind=Super,5,toggleview,5
bind=Super,6,toggleview,6
bind=Super,7,toggleview,7
bind=Super,8,toggleview,8
bind=Super,9,toggleview,9

# monitor switch
bind=alt+shift,Left,focusmon,left
bind=alt+shift,Right,focusmon,right
bind=alt+shift,Up,focusmon,up
bind=alt+shift,Down,focusmon,down
bind=SUPER+Alt,Left,tagmon,left
bind=SUPER+Alt,Right,tagmon,right
bind=SUPER+Alt,Up,tagmon,up
bind=SUPER+Alt,Down,tagmon,down

# gaps
bind=ALT+SHIFT,X,incgaps,1
bind=ALT+SHIFT,Z,incgaps,-1
bind=ALT+SHIFT,R,togglegaps


# Mouse Button Bindings
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=alt,btn_middle,set_proportion,0.5
mousebind=SUPER,btn_right,moveresize,curresize
mousebind=SUPER+CTRL,btn_left,minimized
mousebind=SUPER+CTRL,btn_right,killclient
mousebind=SUPER+CTRL,btn_middle,togglefullscreen
mousebind=NONE,btn_middle,togglemaximizescreen,0

# Axis Bindings
axisbind=SUPER,UP,viewtoleft_have_client
axisbind=SUPER,DOWN,viewtoright_have_client
axisbind=alt,UP,focusdir,left
axisbind=alt,DOWN,focusdir,right
axisbind=shift+super,UP,exchange_client,left
axisbind=shift+super,DOWN,exchange_client,right

# Gesturebind
gesturebind=none,left,3,focusdir,left
gesturebind=none,right,3,focusdir,right
gesturebind=none,up,3,focusdir,up
gesturebind=none,down,3,focusdir,down
gesturebind=none,left,4,viewtoleft_have_client
gesturebind=none,right,4,viewtoright_have_client
gesturebind=none,up,4,toggleoverview,1
gesturebind=none,down,4,toggleoverview,1

# --- Control de Volumen (Salida de audio / Parlantes) ---
bind=none,XF86AudioRaiseVolume,spawn,~/.config/mango/scripts/volume.sh up
bind=none,XF86AudioLowerVolume,spawn,~/.config/mango/scripts/volume.sh down
bind=none,XF86AudioMute,spawn,~/.config/mango/scripts/volume.sh mute

# --- Control de Micrófono (Acer Aspire 5) ---
bind=none,XF86Launch6,spawn,~/.config/mango/scripts/mic.sh

# --- Control de Brillo ---
bind=none,XF86MonBrightnessUp,spawn,~/.config/mango/scripts/brightness.sh up 
bind=none,XF86MonBrightnessDown,spawn,~/.config/mango/scripts/brightness.sh down


#custom app bind
# --- Bloquear Pantalla ---
bind=SUPER,l,spawn, bash ~/.dots/scripts/lock.sh

# namescrtachpad
# bind=ctrl+super,h,toggle_named_scratchpad,st-yazi,none,st -c st-yazi -e yazi
# windowrule=isnamedscratchpad:1,width:1280,height:800,appid:st-yazi


# ----MIS CONFIGURACIONES PERSONALES-----


# Lanzar el selector de fondo waypaper
bind=SUPER,w,spawn,env GTK_THEME=Adwaita:dark waypaper

# Captura de pantalla
bind=SUPER+SHIFT,s,spawn,bash ~/.dots/scripts/screenshot.sh region
bind=NONE,Print,spawn,bash ~/.dots/scripts/screenshot.sh full


#-----ATAJOS DE TECLADO PARA UTILERIA DE ROFI------

# Menu maestro
#bind = Alt,k,spawn, bash ~/.dots/scripts/rofi_menu_selector.sh
bind = Alt,k,spawn, rofi -show menu -modi "menu:$HOME/.dots/scripts/rofi-menu.sh" -theme ~/.config/rofi/launchers/selector_menu/rofi-menu.rasi

# Lanzar powermenu
bind = SUPER,x,spawn, bash ~/.dots/scripts/rofi_powermenu.sh





